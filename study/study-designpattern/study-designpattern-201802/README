#### 研磨设计模式
1. 什么是设计模式
    可以作为模型或者模板的服方法或方式
设计模式分类
    创建型模式：抽象了对象实例化过程， 用来帮助创建对象的实例
    结构型模式：描述如何组合类和对象对以获得更大的结构
    行为型模式：描述算法和对象间职责的分配


example02(简单工厂模式)
    简单工厂的本质就是选择
    简单工厂的方法通常是静态的，所以也被称为静态工厂
    简单工厂优点：
        1. 帮助封装， 封装组件， 然后让外部能真正的面向接口编程
        2. 通过简单工厂，实现了客户端与具体实现类的解耦
    简单工厂缺点：
        1. 增加了客户端复杂程度
        2. 不方便扩展子工厂

example03(外观模式）
    外观模式的本质是：封装交互， 简化调用
    目的：不给子系统添加新的功能， 而是让外部系统减少与子系统内的模块的交互，松耦合，让外部系统能够更简单的使用子系统
    外观模式优点：
        1. 松耦合， 让子系统总内部更加容易扩展
        2. 更好的划分访问层次， 把需要暴露的方法集中到外观中， 很好的隐藏了内部实现


example04（适配器模式）
    定义：将一个接口转换为客户希望的接口解决方案就是适配器模式
